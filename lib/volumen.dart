import 'package:flutter/material.dart';

class Volumen extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(//

      appBar: AppBar(//para poner nombre a los widgets

        title: Text("Conversor De Volumen"),

      ),

    );
  }
} 